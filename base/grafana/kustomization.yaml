# Kustomize resources for Grafana — compliance dashboard UI.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
