# Kustomize resources for Collector — the OpenTelemetry data pipeline.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: collector-config
    files:
      - otel-collector.yaml
