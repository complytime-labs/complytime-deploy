name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  megalinter:
    name: MegaLinter
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Run MegaLinter
        id: megalinter
        uses: trevor-vaughan/megalint-config@latest # zizmor: ignore[unpinned-uses]
        with:
          validate-all-codebase: ${{ github.event_name == 'push' && 'true' || 'false' }}

      - name: Upload MegaLinter reports
        if: ${{ success() || failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: megalinter-reports
          path: |
            ${{ steps.megalinter.outputs.reports-dir }}
            mega-linter.log
          retention-days: 7

      - name: Upload SARIF to Code Scanning
        if: ${{ (success() || failure()) && steps.megalinter.outputs.sarif-has-results == 'true' }}
        uses: github/codeql-action/upload-sarif@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          sarif_file: ${{ steps.megalinter.outputs.sarif-file }}
          category: megalinter
