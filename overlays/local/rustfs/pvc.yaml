# Persistent storage for RustFS bucket data.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
