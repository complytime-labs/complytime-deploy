# Production Grafana env vars — OIDC authentication + root URL.
# Dynamic values (client ID, auth URLs, root URL, admin password) are
# injected from the grafana-env ConfigMap and grafana-admin-secret Secret,
# created by apply-sealed-secrets.sh from GitLab CI variables.
# Set CI variables in GitLab CI (Settings > CI/CD > Variables, scope: production).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  template:
    spec:
      containers:
        - name: grafana
          env:
            - name: GF_AUTH_ANONYMOUS_ENABLED
              value: "false"
            - name: GF_SERVER_PROTOCOL
              value: "https"
            - name: GF_SERVER_CERT_FILE
              value: "/etc/tls/tls.crt"
            - name: GF_SERVER_CERT_KEY
              value: "/etc/tls/tls.key"
            - name: GF_SERVER_ROOT_URL
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_SERVER_ROOT_URL
            - name: GF_AUTH_GENERIC_OAUTH_ENABLED
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_GENERIC_OAUTH_ENABLED
            - name: GF_AUTH_GENERIC_OAUTH_NAME
              value: "Red Hat SSO"
            - name: GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP
              value: "true"
            - name: GF_AUTH_GENERIC_OAUTH_CLIENT_ID
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_GENERIC_OAUTH_CLIENT_ID
            - name: GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: grafana-oidc-secret
                  key: client_secret
            - name: GF_AUTH_GENERIC_OAUTH_SCOPES
              value: "openid profile email"
            - name: GF_AUTH_GENERIC_OAUTH_AUTH_URL
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_GENERIC_OAUTH_AUTH_URL
            - name: GF_AUTH_GENERIC_OAUTH_TOKEN_URL
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_GENERIC_OAUTH_TOKEN_URL
            - name: GF_AUTH_GENERIC_OAUTH_API_URL
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_GENERIC_OAUTH_API_URL
            - name: GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH
              value: "'Viewer'"
            - name: GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_STRICT
              value: "false"
            - name: GF_AUTH_SIGNOUT_REDIRECT_URL
              value: null
              valueFrom:
                configMapKeyRef:
                  name: grafana-env
                  key: GF_AUTH_SIGNOUT_REDIRECT_URL
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-admin-secret
                  key: admin_password
